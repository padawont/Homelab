# Tooling

Developer tools, editors, build systems, and project management.

## Subcategories

- `dev-environments/` — Development environment tools and configuration
- `opencode/` — OpenCode agent and skill configuration
- `version-control/` — Git and version control tooling

## Index

- `dev-environments/devbox/` — Devbox: isolated, reproducible dev shells via Nix
- `dev-environments/direnv/` — direnv: automatic per-directory env loading
- `dev-environments/worktrunk/` — Worktrunk: git worktree management for parallel AI agent sessions
- `opencode/agents/` — OpenCode agent types, configuration, permissions, discovery, context loading
- `opencode/skills/` — OpenCode skill format, configuration, discovery, permissions
- `version-control/git/worktree/` — Git Worktree: multi-branch checkout in isolated directories
