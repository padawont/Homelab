# Implementation — Workflow

Goal: document the real, running setup.

1. Document — `foundation` + `research-implement` + `login-server`
2. Deploy — `deploy-service` (+ `node-overview` to verify)
3. Validate — `loop-validation`
4. Retire — manual → `./06_Archive/implementations/`
