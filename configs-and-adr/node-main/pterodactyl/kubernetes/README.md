# kubernetes/

Pterodactyl Kubernetes manifests. Currently empty — Pterodactyl runs in a Harvester VM, not in K3s.

If Pterodactyl is migrated to Kubernetes in the future, this directory will contain:
- Deployments (Panel, MariaDB, Redis)
- Services
- ConfigMaps
- Ingress rules
- PersistentVolumeClaims
